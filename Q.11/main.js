"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// Store the names of a few of your friends in an array called friendNames.
let friendNames = ["Raheel Khan", "Jamal Khan", "Zarak Khan"];
// Print each person’s name by accessing each element in the list, one at a time.
console.log(friendNames[0]); // Prints "Raheel Khan"
console.log(friendNames[1]); // Prints "Jamal Khan"
console.log(friendNames[2]); // Prints "Zarak Khan"
/*
Arrays: An array is a data structure that can hold multiple values, which are accessed using an index. In JavaScript and TypeScript, arrays are zero-indexed, meaning the first element is accessed with index 0.
*/ 
